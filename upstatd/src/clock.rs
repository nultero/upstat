// use crate::colors;
// use chrono;

// #[allow(dead_code)]
// pub fn print_time(cs: &colors::Colors) {
//     let now = chrono::Local::now();
//     // now.format(fmt)
//     print!("{}, {}", now.timestamp_millis(), cs.chrome);
// }
